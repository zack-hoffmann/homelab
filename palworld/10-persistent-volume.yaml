apiVersion: v1
kind: PersistentVolume
metadata:
  name: palworld-files
  namespace: palworld
spec:
  capacity:
    storage: 12Gi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  nfs:
    server: grist.the-hoffmanns.org
    path: /data/palworld
