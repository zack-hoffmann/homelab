apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-staging-test
  namespace: default
spec:
  secretName: home-staging-test-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - home.the-hoffmanns.org